import Example from '@/components/Example'
import Link from 'next/link'
import React from 'react'

const Page = () => {
  return <article className={['container'].filter(Boolean).join(' ')}></article>
}

export default Page
